# The public demo works without any environment variables.
EXTRACTION_PROVIDER=demo

# Optional local live extraction. Never expose this value to browser code.
# EXTRACTION_PROVIDER=gemini
# GEMINI_API_KEY=replace_with_your_own_server_side_key
# GEMINI_MODEL=gemini-2.5-flash
