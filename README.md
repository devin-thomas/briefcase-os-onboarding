# BriefcaseOS Onboarding

A clean-room, portfolio-safe rebuild of the BriefcaseOS candidate onboarding experience.

> This repository was initialized with fresh history. Private infrastructure, credentials, personal fixtures, and internal deployment material are intentionally excluded.

Implementation is in progress on a feature branch. The hosted public demo will default to deterministic fictional data and will not require an API key or real resume.
